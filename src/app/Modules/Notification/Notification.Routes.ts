import { Router } from "express";
import { AuthControllers } from "./Notification.Controller";

const router = Router();

// router.post("/create-notification", AuthControllers.LoginUser);
// router.post("/logout", AuthControllers.LogOutUser);
// const Notification = router;
// export default AuthRoutes;
